postgis:
  build: ./dockerfiles/postgres
  environment:
    PASS: working_waterfronts
    USERNAME: working_waterfronts

web:
  build: ./dockerfiles/working-waterfronts
  ports:
    - "8000:8000"
  volumes:
    - .:/opt/working_waterfronts
  links:
    - postgis
